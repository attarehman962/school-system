# PostgreSQL (LAPP). Leave empty and set USE_SQLITE=1 to use SQLite instead.
DATABASE_URL=postgresql+psycopg://myuser:mypassword@localhost:5432/school_db

# Use SQLite (no PostgreSQL needed). Data stored in instance/school.db
# USE_SQLITE=1

SECRET_KEY=change-me

