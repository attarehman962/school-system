# PostgreSQL (uncomment when Postgres is running)
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/school_db

# Use SQLite so the app works without PostgreSQL
USE_SQLITE=1

SECRET_KEY=change-me

